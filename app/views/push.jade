doctype html
html
  head(profile='http://a9.com/-/spec/opensearch/1.1/')
    include _parts/header.jade
    title #{__(title)} | #{appdata.name}
    
  body
    header
      .navbar-fixed
        nav(class=appdata.ui.theme.baseColor, role='navigation')
          .nav-wrapper.container
            span#logo-container.brand-logo.truncate #{__('Push to %s', appdata.name)}
          
    main
      .container
        if success
          script.
            window.close();
        else
          .row
            include _parts/form/add.jade
        
    script(src='/assets/jquery/dist/jquery.min.js')
    script(src='/assets/materialize/dist/js/materialize.min.js')
    script(src='/js/push.#{appdata.options.jsExtension}')