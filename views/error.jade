extends _layout

block header
  title Error #{status} | #{appdata.name}
  

block body
  .section.no-pad-bot
    h1.header.center Yeah! Somethings broken!
    p.center.flow-text #{status} - #{message}
    
  if (error) 
    h2 Error